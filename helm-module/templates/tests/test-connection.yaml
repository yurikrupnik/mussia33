apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-module.fullname" . }}-test-connection"
  labels:
    {{- include "helm-module.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-module.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
