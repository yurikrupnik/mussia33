apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-module.fullname" . }}
  labels:
    {{- include "helm-module.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-module.selectorLabels" . | nindent 4 }}
