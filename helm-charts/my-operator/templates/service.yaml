apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-operator.fullname" . }}
  labels:
    {{- include "my-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-operator.selectorLabels" . | nindent 4 }}
